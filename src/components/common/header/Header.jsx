import { StyledHeader, StyledTitle } from "./StyledHeader";

const Header = () => {
  return (
    <StyledHeader>
      <StyledTitle>BR</StyledTitle>
    </StyledHeader>
  );
};

export default Header;
